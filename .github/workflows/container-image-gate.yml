name: Container Image Gate

on:
  push:
    branches:
      - master
      - main
      - "cursor/**"
    paths:
      - ".github/workflows/container-image-gate.yml"
      - ".docker/Dockerfile.cli"
      - "application/src/Ashlar.CLI/**"
      - "src/Ashlar.Core.Application/**"
      - "src/Ashlar.Infrastructure/**"
      - "src/Ashlar.Hosting/**"
      - "src/Ashlar.Runtime/**"
      - "Directory.Build.props"
      - "Directory.Packages.props"
      - "global.json"
      - "README.md"
  workflow_dispatch:

jobs:
  container-image-gate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build local amd64 image
        shell: bash
        run: |
          docker build \
            --file .docker/Dockerfile.cli \
            --tag ashlar-cli:gate-amd64 \
            .

      - name: Smoke run amd64 image
        shell: bash
        run: |
          docker run --rm ashlar-cli:gate-amd64 --help

      - name: Validate multi-arch buildability (no push)
        shell: bash
        run: |
          docker buildx build \
            --file .docker/Dockerfile.cli \
            --platform linux/amd64,linux/arm64 \
            --tag ashlar-cli:gate-multiarch \
            --output type=cacheonly \
            .
