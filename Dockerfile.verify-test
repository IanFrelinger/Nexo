FROM mcr.microsoft.com/dotnet/sdk:8.0

WORKDIR /app

# Copy only the verification test
COPY verification-test/ .

# Build the verification test
RUN dotnet build -c Release

# Set up the entrypoint
ENTRYPOINT ["bash", "-lc"]
