[
  {
    "fixture": "T10_IndexedMultiSection",
    "note": "TOC-indexed multi-section archive",
    "extractMs": 552,
    "adaptMs": 21491,
    "lineCount": 61,
    "knownSymbolsFound": [
      "FlightRecorderArchive",
      "nextRecord"
    ],
    "knownSymbolsTotal": 4,
    "missingSymbols": [
      "selectSection",
      "advanceToNextSection"
    ],
    "overridesFound": 6,
    "overridesTotal": 6,
    "targetsContract": true,
    "code": "#pragma once\n#include \"processing.hpp\"\n#include <memory>\n\nnamespace fileproc {\n\nclass CustomEventReader : public IEventReader {\npublic:\n    explicit CustomEventReader(const std::filesystem::path& path) {\n        // open `path` using the proprietary parser's real constructor/open call\n        in_.open(path, std::ios::binary);\n        if (!in_) {\n            throw std::runtime_error(\"Failed to open file\");\n        }\n    }\n\n    void set_requested_fields(const std::vector<std::string>& names) override {\n        // remember which named fields to decode per-record, if the format is non-flat\n    }\n    bool decodes_named_fields() const override { return true; } // false = flat out.data + offsets instead\n\n    bool next(Event& out, size_t max_depth) override {\n        // read ONE record using the proprietary parser's real \"read next\" call.\n        // out.t = timestamp (non-decreasing); out.type = type code;\n        // out.data = raw payload bytes (flat formats) OR out.fields[name] = value (non-flat).\n        // return false at end-of-stream.\n        FlightRecord record;\n        bool success = in_.nextRecord(record);\n        if (!success) {\n            return false;\n        }\n        // TODO: convert the raw payload bytes to a flat format (if necessary) and store in out.data\n        // TODO: set out.t to the timestamp from record\n        // TODO: set out.type to the type code from record\n        return true;\n    }\n\n    bool     can_resume() const override { return true; }   // false if the format can't seek\n    uint64_t position()   const override { return in_.positionInSection(); }   // byte/record offset of the NEXT record\n    void     resume_at(uint64_t off) override { in_.seekWithinSection(off); }\n\nprivate:\n    FlightRecorderArchive in_;\n};\n\ninline FileInfo inspect_custom(const std::filesystem::path& path) {\n    // open the file just enough to report header metadata, using the proprietary parser's\n    // real API: FileInfo fi; fi.event_count=...; fi.t_begin=...; fi.t_end=...;\n    // fi.bytes=std::filesystem::file_size(path); fi.resumable=true; fi.types={\"...\", ...};\n    return FileInfo{};\n}\n\ninline void use_custom_reader(){\n    reader_factory() = [](const std::filesystem::path& p){\n        return std::unique_ptr<IEventReader>(new CustomEventReader(p));\n    };\n    inspect_factory() = inspect_custom;\n}\n\n} // namespace fileproc\n"
  },
  {
    "fixture": "T1_TrivialCounter",
    "note": "in-memory only, no I/O, no seek",
    "extractMs": 639,
    "adaptMs": 9454,
    "lineCount": 49,
    "knownSymbolsFound": [],
    "knownSymbolsTotal": 2,
    "missingSymbols": [
      "SimpleCounter",
      "advance"
    ],
    "overridesFound": 6,
    "overridesTotal": 6,
    "targetsContract": true,
    "code": "#pragma once\n#include \"processing.hpp\"\n#include <memory>\n\nnamespace fileproc {\n\nclass CustomEventReader : public IEventReader {\npublic:\n    explicit CustomEventReader(const std::filesystem::path& path) {\n        // open `path` using the proprietary parser's real constructor/open call\n    }\n\n    void set_requested_fields(const std::vector<std::string>& names) override {\n        // remember which named fields to decode per-record, if the format is non-flat\n    }\n    bool decodes_named_fields() const override { return true; } // false = flat out.data + offsets instead\n\n    bool next(Event& out, size_t max_depth) override {\n        // read ONE record using the proprietary parser's real \"read next\" call.\n        // out.t = timestamp (non-decreasing); out.type = type code;\n        // out.data = raw payload bytes (flat formats) OR out.fields[name] = value (non-flat).\n        // return false at end-of-stream.\n    }\n\n    bool     can_resume() const override { return true; }   // false if the format can't seek\n    uint64_t position()   const override { return pos_; }   // byte/record offset of the NEXT record\n    void     resume_at(uint64_t off) override { /* seek using the proprietary parser's real seek call */ }\n\nprivate:\n    uint64_t pos_ = 0;\n    // real stream/handle members from the proprietary parser go here\n};\n\ninline FileInfo inspect_custom(const std::filesystem::path& path) {\n    // open the file just enough to report header metadata, using the proprietary parser's\n    // real API: FileInfo fi; fi.event_count=...; fi.t_begin=...; fi.t_end=...;\n    // fi.bytes=std::filesystem::file_size(path); fi.resumable=true; fi.types={\"...\", ...};\n    return FileInfo{};\n}\n\ninline void use_custom_reader(){\n    reader_factory() = [](const std::filesystem::path& p){\n        return std::unique_ptr<IEventReader>(new CustomEventReader(p));\n    };\n    inspect_factory() = inspect_custom;\n}\n\n} // namespace fileproc\n"
  },
  {
    "fixture": "T2_FixedRecordLog",
    "note": "fixed-size records, sequential-only (no seek exists)",
    "extractMs": 2973,
    "adaptMs": 24319,
    "lineCount": 35,
    "knownSymbolsFound": [],
    "knownSymbolsTotal": 2,
    "missingSymbols": [
      "FixedRecordLog",
      "readRecord"
    ],
    "overridesFound": 6,
    "overridesTotal": 6,
    "targetsContract": true,
    "code": "#pragma once\n#include \"processing.hpp\"\n#include <memory>\n\nnamespace fileproc {\n\nclass CustomEventReader : public IEventReader {\npublic:\n    explicit CustomEventReader(const std::filesystem::path& path) {\n        // TODO: open `path` using the proprietary parser's real constructor/open call\n    }\n\n    void set_requested_fields(const std::vector<std::string>& names) override {\n        // TODO: remember which named fields to decode per-record, if the format is non-flat\n    }\n    bool decodes_named_fields() const override { return true; } // false = flat out.data + offsets instead\n\n    bool next(Event& out, size_t max_depth) override {\n        // TODO: read ONE record using the proprietary parser's real \"read next\" call.\n        // out.t = timestamp (non-decreasing); out.type = type code;\n        // out.data = raw payload bytes (flat formats) OR out.fields[name] = value (non-flat).\n        // return false at end-of-stream.\n    }\n\n    bool     can_resume() const override { return true; }   // TODO: false if the format can't seek\n    uint64_t position()   const override { return pos_; }   // byte/record offset of the NEXT record\n    void     resume_at(uint64_t off) override { /* TODO: seek using the proprietary parser's real seek call */ }\n\nprivate:\n    uint64_t pos_ = 0;\n    // TODO: real stream/handle members from the proprietary parser go here\n};\n\n} // namespace fileproc\n"
  },
  {
    "fixture": "T3_HeaderedFixedLog",
    "note": "fixed records + header + real seek",
    "extractMs": 644,
    "adaptMs": 17471,
    "lineCount": 63,
    "knownSymbolsFound": [
      "TelemetryStream",
      "fetchNext"
    ],
    "knownSymbolsTotal": 4,
    "missingSymbols": [
      "jumpTo",
      "tell"
    ],
    "overridesFound": 6,
    "overridesTotal": 6,
    "targetsContract": true,
    "code": "#pragma once\n#include \"processing.hpp\"\n#include <memory>\n\nnamespace fileproc {\n\nclass CustomEventReader : public IEventReader {\npublic:\n    explicit CustomEventReader(const std::filesystem::path& path) {\n        // open `path` using the proprietary parser's real constructor/open call\n        in_ = std::make_unique<TelemetryStream>(path);\n        count_ = in_->sampleCount();\n    }\n\n    void set_requested_fields(const std::vector<std::string>& names) override {\n        // remember which named fields to decode per-record, if the format is non-flat\n        // TODO: implement this method if needed (not required for flat out.data + offsets)\n    }\n    bool decodes_named_fields() const override { return false; } // flat out.data + offsets instead\n\n    bool next(Event& out, size_t max_depth) override {\n        // read ONE record using the proprietary parser's real \"read next\" call.\n        // out.t = timestamp (non-decreasing); out.type = type code;\n        // out.data = raw payload bytes (flat formats) OR out.fields[name] = value (non-flat).\n        // return false at end-of-stream.\n        if (pos_ >= count_) {\n            return false;\n        }\n        TelemetrySample sample;\n        in_->fetchNext(sample);\n        out.t = sample.timestampSec;\n        out.type = 0; // TODO: set the type code, if needed\n        out.data = std::vector<uint8_t>(reinterpret_cast<const uint8_t*>(&sample), reinterpret_cast<const uint8_t*>(&sample + 1));\n        pos_++;\n        return true;\n    }\n\n    bool     can_resume() const override { return true; }   // false if the format can't seek\n    uint64_t position()   const override { return pos_; }   // byte/record offset of the NEXT record\n    void     resume_at(uint64_t off) override { /* seek using the proprietary parser's real seek call */ }\n\nprivate:\n    std::unique_ptr<TelemetryStream> in_;\n    uint32_t count_ = 0;\n    uint64_t pos_ = 0;\n};\n\ninline FileInfo inspect_custom(const std::filesystem::path& path) {\n    // open the file just enough to report header metadata, using the proprietary parser's\n    // real API: FileInfo fi; fi.event_count=...; fi.t_begin=...; fi.t_end=...;\n    // fi.bytes=std::filesystem::file_size(path); fi.resumable=true; fi.types={\"...\", ...};\n    return FileInfo{};\n}\n\ninline void use_custom_reader(){\n    reader_factory() = [](const std::filesystem::path& p){\n        return std::unique_ptr<IEventReader>(new CustomEventReader(p));\n    };\n    inspect_factory() = inspect_custom;\n}\n\n} // namespace fileproc\n"
  },
  {
    "fixture": "T4_VariableLengthTLV",
    "note": "variable-length TLV records",
    "extractMs": 672,
    "adaptMs": 14593,
    "lineCount": 55,
    "knownSymbolsFound": [
      "TlvEventStream",
      "nextEvent",
      "seekBytes"
    ],
    "knownSymbolsTotal": 3,
    "missingSymbols": [],
    "overridesFound": 6,
    "overridesTotal": 6,
    "targetsContract": true,
    "code": "#pragma once\n#include \"processing.hpp\"\n#include <memory>\n\nnamespace fileproc {\n\nclass CustomEventReader : public IEventReader {\npublic:\n    explicit CustomEventReader(const std::filesystem::path& path) {\n        // open `path` using the proprietary parser's real constructor/open call\n        in_ = std::make_unique<TlvEventStream>(path);\n    }\n\n    void set_requested_fields(const std::vector<std::string>& names) override {\n        // remember which named fields to decode per-record, if the format is non-flat\n        // UNCERTAIN: how to map this method?\n    }\n    bool decodes_named_fields() const override { return true; } // false = flat out.data + offsets instead\n\n    bool next(Event& out, size_t max_depth) override {\n        // read ONE record using the proprietary parser's real \"read next\" call.\n        TlvEvent evt;\n        if (!in_->nextEvent(evt)) return false;\n        out.t = evt.t;\n        out.type = 0; // UNCERTAIN: how to map the event type?\n        out.data = std::move(evt.value);\n        pos_ += in_->bytesRead();\n        return true;\n    }\n\n    bool     can_resume() const override { return true; }   // false if the format can't seek\n    uint64_t position()   const override { return pos_; }   // byte/record offset of the NEXT record\n    void     resume_at(uint64_t off) override { in_->seekBytes(off); pos_ = off; }\n\nprivate:\n    uint64_t pos_ = 0;\n    std::unique_ptr<TlvEventStream> in_;\n};\n\ninline FileInfo inspect_custom(const std::filesystem::path& path) {\n    // open the file just enough to report header metadata, using the proprietary parser's\n    // real API: FileInfo fi; fi.event_count=...; fi.t_begin=...; fi.t_end=...;\n    // fi.bytes=std::filesystem::file_size(path); fi.resumable=true; fi.types={\"...\", ...};\n    return FileInfo{};\n}\n\ninline void use_custom_reader(){\n    reader_factory() = [](const std::filesystem::path& p){\n        return std::unique_ptr<IEventReader>(new CustomEventReader(p));\n    };\n    inspect_factory() = inspect_custom;\n}\n\n} // namespace fileproc\n"
  },
  {
    "fixture": "T5_MultiClassCursor",
    "note": "two-class indirection (must hold the cursor, not the log)",
    "extractMs": 660,
    "adaptMs": 8943,
    "lineCount": 35,
    "knownSymbolsFound": [
      "seek"
    ],
    "knownSymbolsTotal": 4,
    "missingSymbols": [
      "SessionLog",
      "SessionCursor",
      "openCursor"
    ],
    "overridesFound": 6,
    "overridesTotal": 6,
    "targetsContract": true,
    "code": "#pragma once\n#include \"processing.hpp\"\n#include <memory>\n\nnamespace fileproc {\n\nclass CustomEventReader : public IEventReader {\npublic:\n    explicit CustomEventReader(const std::filesystem::path& path) {\n        // open `path` using the proprietary parser's real constructor/open call\n    }\n\n    void set_requested_fields(const std::vector<std::string>& names) override {\n        // remember which named fields to decode per-record, if the format is non-flat\n    }\n    bool decodes_named_fields() const override { return true; } // false = flat out.data + offsets instead\n\n    bool next(Event& out, size_t max_depth) override {\n        // read ONE record using the proprietary parser's real \"read next\" call.\n        // out.t = timestamp (non-decreasing); out.type = type code;\n        // out.data = raw payload bytes (flat formats) OR out.fields[name] = value (non-flat).\n        // return false at end-of-stream.\n    }\n\n    bool     can_resume() const override { return true; }   // false if the format can't seek\n    uint64_t position()   const override { return pos_; }   // byte/record offset of the NEXT record\n    void     resume_at(uint64_t off) override { /* seek using the proprietary parser's real seek call */ }\n\nprivate:\n    uint64_t pos_ = 0;\n    // real stream/handle members from the proprietary parser go here\n};\n\n} // namespace fileproc\n"
  },
  {
    "fixture": "T6_AbstractBaseHierarchy",
    "note": "abstract base + concrete subclass",
    "extractMs": 664,
    "adaptMs": 10920,
    "lineCount": 47,
    "knownSymbolsFound": [
      "BinaryRecordSource",
      "pull"
    ],
    "knownSymbolsTotal": 3,
    "missingSymbols": [
      "seekTo"
    ],
    "overridesFound": 6,
    "overridesTotal": 6,
    "targetsContract": true,
    "code": "#pragma once\n#include \"processing.hpp\"\n#include <memory>\n\nnamespace fileproc {\n\nclass CustomEventReader : public IEventReader {\npublic:\n    explicit CustomEventReader(const std::filesystem::path& path) {\n        // open `path` using the proprietary parser's real constructor/open call\n        in_ = std::make_unique<BinaryRecordSource>(path.string());\n    }\n\n    void set_requested_fields(const std::vector<std::string>& names) override {\n        // remember which named fields to decode per-record, if the format is non-flat\n        requested_fields_ = names;\n    }\n    bool decodes_named_fields() const override { return true; } // false = flat out.data + offsets instead\n\n    bool next(Event& out, size_t max_depth) override {\n        // read ONE record using the proprietary parser's real \"read next\" call.\n        // out.t = timestamp (non-decreasing); out.type = type code;\n        // out.data = raw payload bytes (flat formats) OR out.fields[name] = value (non-flat).\n        // return false at end-of-stream.\n        if (!in_->pull(out.record)) {\n            return false;\n        }\n        out.t = out.record.t;\n        out.type = 0; // TODO: determine event type from record.bytes or record.fields\n        for (const auto& field : requested_fields_) {\n            // TODO: fill out.fields[field] with the value of the named field, if present in the record.\n            // If not, leave it empty.\n        }\n        pos_ = in_->currentOffset();\n        return true;\n    }\n\n    bool     can_resume() const override { return false; }   // TODO: determine seekability from the input stream\n    uint64_t position()   const override { return pos_; }   // byte/record offset of the NEXT record\n    void     resume_at(uint64_t off) override { /* UNCERTAIN: seek using the proprietary parser's real seek call */ }\n\nprivate:\n    uint64_t pos_ = 0;\n    std::unique_ptr<BinaryRecordSource> in_; // TODO: replace with the appropriate class/struct from the proprietary parser's API\n    std::vector<std::string> requested_fields_;\n};\n"
  },
  {
    "fixture": "T7_StatefulMultiPass",
    "note": "explicit open() lifecycle; seek is by RECORD INDEX not bytes",
    "extractMs": 565,
    "adaptMs": 11430,
    "lineCount": 46,
    "knownSymbolsFound": [
      "ArchiveReader",
      "open"
    ],
    "knownSymbolsTotal": 4,
    "missingSymbols": [
      "readNext",
      "seekRecord"
    ],
    "overridesFound": 6,
    "overridesTotal": 6,
    "targetsContract": true,
    "code": "#pragma once\n#include \"processing.hpp\"\n#include <memory>\n\nnamespace fileproc {\n\nclass CustomEventReader : public IEventReader {\npublic:\n    explicit CustomEventReader(const std::filesystem::path& path) : path_(path) {\n        // TODO: Implement the constructor that opens the file using the proprietary parser's real API.\n    }\n\n    void set_requested_fields(const std::vector<std::string>& names) override {\n        // TODO: Implement the `set_requested_fields` method to remember which named fields to decode per-record, if the format is non-flat.\n    }\n\n    bool decodes_named_fields() const override {\n        // TODO: Implement the `decodes_named_fields` method to return false if the format can't seek, true otherwise.\n    }\n\n    bool next(Event& out, size_t max_depth) override {\n        // TODO: Implement the `next` method to read ONE record using the proprietary parser's real \"read next\" call.\n        // Out.t = timestamp (non-decreasing); out.type = type code;\n        // Out.data = raw payload bytes (flat formats) OR out.fields[name] = value (non-flat).\n        // Return false at end-of-stream.\n    }\n\n    bool     can_resume() const override {\n        // TODO: Implement the `can_resume` method to return true if the format supports seeking, false otherwise.\n    }\n\n    uint64_t position()   const override {\n        // TODO: Implement the `position` method to return the byte/record offset of the NEXT record.\n    }\n\n    void     resume_at(uint64_t off) override {\n        // TODO: Implement the `resume_at` method to seek using the proprietary parser's real seek call.\n    }\n\nprivate:\n    std::string path_;\n    std::unique_ptr<ArchiveReader> reader_;\n};\n\n} // namespace fileproc\n"
  },
  {
    "fixture": "T8_TemplatedRecordType",
    "note": "header-only template, must pick a concrete instantiation",
    "extractMs": 480,
    "adaptMs": 15236,
    "lineCount": 57,
    "knownSymbolsFound": [
      "SensorLog",
      "readTyped"
    ],
    "knownSymbolsTotal": 3,
    "missingSymbols": [
      "rewindTo"
    ],
    "overridesFound": 6,
    "overridesTotal": 6,
    "targetsContract": true,
    "code": "#pragma once\n#include \"processing.hpp\"\n#include <memory>\n\nnamespace fileproc {\n\nclass CustomEventReader : public IEventReader {\npublic:\n    explicit CustomEventReader(const std::filesystem::path& path) {\n        // open `path` using the proprietary parser's real constructor/open call\n        in_ = new SensorLog(path);\n    }\n\n    void set_requested_fields(const std::vector<std::string>& names) override {\n        // remember which named fields to decode per-record, if the format is non-flat\n    }\n    bool decodes_named_fields() const override { return true; } // false = flat out.data + offsets instead\n\n    bool next(Event& out, size_t max_depth) override {\n        // read ONE record using the proprietary parser's real \"read next\" call.\n        // out.t = timestamp (non-decreasing); out.type = type code;\n        // out.data = raw payload bytes (flat formats) OR out.fields[name] = value (non-flat).\n        double t = 0;\n        SensorPayload payload;\n        if (!in_->readTyped(t, payload)) return false;\n        out.t = t;\n        out.type = 1;\n        // TODO: encode the payload into out.data or out.fields based on non-flat/flat decision\n        pos_ += in_->offset();\n        return true;\n    }\n\n    bool     can_resume() const override { return false; }   // false if the format can't seek\n    uint64_t position()   const override { return pos_; }   // byte/record offset of the NEXT record\n    void     resume_at(uint64_t off) override { /* seek using the proprietary parser's real seek call */ }\n\nprivate:\n    uint64_t pos_ = 0;\n    SensorLog* in_; // TODO: replace with std::unique_ptr<SensorLog> or other RAII type if necessary\n};\n\ninline FileInfo inspect_custom(const std::filesystem::path& path) {\n    // open the file just enough to report header metadata, using the proprietary parser's\n    // real API: FileInfo fi; fi.event_count=...; fi.t_begin=...; fi.t_end=...;\n    // fi.bytes=std::filesystem::file_size(path); fi.resumable=true; fi.types={\"...\", ...};\n    return FileInfo{};\n}\n\ninline void use_custom_reader(){\n    reader_factory() = [](const std::filesystem::path& p){\n        return std::unique_ptr<IEventReader>(new CustomEventReader(p));\n    };\n    inspect_factory() = inspect_custom;\n}\n\n} // namespace fileproc\n"
  },
  {
    "fixture": "T9_ChunkedCompressed",
    "note": "private buffer/decompress internals; seek is chunk-granular",
    "extractMs": 562,
    "adaptMs": 9234,
    "lineCount": 32,
    "knownSymbolsFound": [
      "ChunkedTraceLog",
      "nextTraceEvent",
      "seekToChunk"
    ],
    "knownSymbolsTotal": 3,
    "missingSymbols": [],
    "overridesFound": 6,
    "overridesTotal": 6,
    "targetsContract": true,
    "code": "#pragma once\n#include \"processing.hpp\"\n#include <memory>\n\nnamespace fileproc {\n\nclass CustomEventReader : public IEventReader {\npublic:\n    explicit CustomEventReader(const std::filesystem::path& path) : chunkedTraceLog_(path) {}\n\n    void set_requested_fields(const std::vector<std::string>& names) override { /* ignore */ }\n    bool decodes_named_fields() const override { return false; }\n\n    bool next(Event& out, size_t max_depth) override {\n        TraceEvent event;\n        if (!chunkedTraceLog_.nextTraceEvent(event)) return false;\n        out.t = event.t;\n        out.type = static_cast<uint8_t>(event.code);\n        out.data = std::vector<uint8_t>(); // TODO: fill this with the raw payload bytes\n        return true;\n    }\n\n    bool     can_resume() const override { return chunkedTraceLog_.currentChunk() < fileproc::MAX_CHUNKS; }   // false if the format can't seek\n    uint64_t position()   const override { /* TODO: implement this */ return 0; }\n    void     resume_at(uint64_t off) override { chunkedTraceLog_.seekToChunk(off); }\n\nprivate:\n    fileproc::ChunkedTraceLog chunkedTraceLog_;\n};\n\n} // namespace fileproc\n"
  }
]