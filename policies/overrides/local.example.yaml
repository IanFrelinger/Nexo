# Copy to policies/overrides/local.yaml (gitignore this file)
# Developer- or repo-specific tweaks:
gates:
  tests:
    min_coverage: 0.70   # lower locally
style:
  max_warnings: 200
sandbox:
  network:
    default: allow       # e.g., for exploratory local runs
