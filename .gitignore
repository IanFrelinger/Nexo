# DocFX API docs output
docs/api/_site/
docs/api/api/

# Build artifacts
**/bin/
**/obj/
artifacts/
# Nested helper tool under Ashlar.Tests.Infrastructure (never commit its build output)
src/Ashlar.Tests.Infrastructure/scripts/bin/
src/Ashlar.Tests.Infrastructure/scripts/obj/
*.dll
*.pdb
*.exe
*.cache

# Database files
*.db
*.db-shm
*.db-wal

# Log files
*.log
logs/

# Temporary files
*.tmp
*.temp
*.bak
*.swp
*.swo
*~

# IDE / editor (Unity tooling removed from repo; keep editor cruft ignored)
.vs/
.vscode/
.idea/
*.user
*.suo
*.sln.docstates
*.userprefs

# OS files
.DS_Store
Thumbs.db

# Test results
TestResults/
CoverageReports/
test-results/
test-reports/
*.trx
*.coverage
coverage.opencover.xml
*.opencover.xml
*.dmp

# Package files
packages/
*.nupkg

# Node modules (if any)
node_modules/

# Python (if any)
__pycache__/
*.pyc
*.pyo

# Unreal Engine (if any)
Binaries/
Intermediate/
Saved/
DerivedDataCache/

# Archive directories
archive/
backup/
/test-reports/
appsettings.local.json
.env.friend-mesh
.env.mesh-lab
.mesh-lab-tls-certs/

/ASHLAR_AGENT_NOTES.md

# Experimental self-extension / Unity scaffolding artifacts
docs/UnityBootstrapGenerated/
docs/PersonalAppGenerated/
docs/UiDomainDemoGenerated/
src/Ashlar.CLI/Commands/SelfExtendGenerated/
src/Ashlar.Tests.CLI/Tests/Commands/SelfExtendGenerated/
tools/unity-demo-output/
tools/ui-demo-output/

# Ashlar runtime workspace (agent state, logs, scratchpads, optimizer reports)
.ashlar/

# WIP self-extension stub generated by agents
src/studio/

# Local compose / agent-server secrets and machine paths
.env
.env.*
!.env.example
!docs/config/agent-server.env.example

# Personal compose overrides (docs/SelfHostedAgentServer.md, "Local compose overrides"). These carry
# host paths and secrets-file mounts, so they must never be tracked. The shipped Docker Desktop
# override deploy/compose/docker-compose.agent-server.local.yml is a different thing -- it is part of
# the repo and deliberately not matched here.
deploy/compose/local/


# Generated local artifacts
brick.pp

# Claude Code machine-local permission grants (overnight autonomy)
.claude/settings.local.json

# Parallel agent worktrees (local only)
.worktrees/
