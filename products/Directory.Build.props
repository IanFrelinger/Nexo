<Project>
  <!-- Chain to the repo-root Directory.Build.props; MSBuild only auto-imports the nearest one. -->
  <Import Project="$([MSBuild]::GetPathOfFileAbove('Directory.Build.props', '$(MSBuildThisFileDirectory)../'))" />
</Project>
