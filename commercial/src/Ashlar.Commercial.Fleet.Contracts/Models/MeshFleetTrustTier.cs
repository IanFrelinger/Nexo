namespace Ashlar.Commercial.Fleet.Contracts.Models;

/// <summary>Trust label for a fleet worker registered with the mesh director (Phase 5 placement policy).</summary>
public enum MeshFleetTrustTier
{
    Unknown = 0,
    Untrusted = 1,
    Trusted = 2
}
