# Optional: relative csproj paths (posix) that pack-nexo-hosting-graph.{sh,ps1} may pack
# even when they are not transitive ProjectReferences of Nexo.Hosting.
# Keep this file empty unless you intentionally ship extra Nexo.* packages in the same graph.
